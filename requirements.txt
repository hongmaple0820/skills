# Skills 项目依赖

# 核心依赖
# (当前版本使用 Python 标准库，无需额外依赖)

# 未来可能需要的依赖:
# FastAPI>=0.100.0    # Web API
# SQLAlchemy>=2.0.0   # 数据库 ORM
# pytest>=7.0.0       # 测试框架
# click>=8.0.0        # CLI 框架 (可选替代 argparse)
